package pages

import (
	"github.com/theSamuelIyeh/johnethel-school-rms/views/partials"
)

templ DashboardClassesComponent() {
	<div class="">
		@partials.Header1(templ.Attributes{"x-html": "'<span class=\"font-normal\">this is classes, </span>' +" + "userName"})
		<div class="w-full overflow-x-auto">
			// @table()
		</div>
	</div>
}
